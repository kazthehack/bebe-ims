import React from 'react'

export const EmployeeProfileView = () => (
  <div style={{ padding: '16px' }}>
    Employee profile view is available in legacy reference and will be reconnected during migration.
  </div>
)
