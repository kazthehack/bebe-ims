server {
    listen 80;
    listen [::]:80;
    server_name localhost;
    root /app/build;

    location ^~ /static/ {
        index index.html index.htm;
    }

    location / {
        try_files $uri /index.html =404;
    }
}
